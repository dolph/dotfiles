- name: install ripgrep
  ansible.builtin.dnf:
    name: ripgrep

- name: configure ripgrep
  ansible.builtin.template:
    src: home/ripgreprc
    dest: "/home/{{ whoami }}/.ripgreprc"
    owner: "{{ whoami }}"
    group: "{{ whoami }}"
