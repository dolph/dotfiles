#!/bin/bash

function lastworkingday()
{
    if [[ "1" == "$(date +%u)" ]]
    then
        echo "last friday"
    else
        echo "yesterday"
    fi
}

git log --since="$(lastworkingday)" --pretty="format:%h %s - %an" | ack -A 3 Dolph
