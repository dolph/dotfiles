[Unit]
Description=Reboot automatically when needed
Wants=reboot.timer

[Service]
Type=oneshot
ExecStart=/bin/bash -c "if uptime --pretty | grep week ; then dnf needs-restarting -r || shutdown -r; fi"

[Install]
WantedBy=multi-user.target
